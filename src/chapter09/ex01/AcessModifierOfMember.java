package chapter09.ex01;

public class AcessModifierOfMember {

	public static void main(String[] args) {
		// 캡슐화 : 정보를 보호하기 위해서 클래스(public, default) 이름, 생성자(public, default)
			//  필드, 메소드 이름 앞에 붙는 접근 제어자
		/*
		 	public	  : 다른 패키지에서 접근이 가능하도록
		 	protected : 다른 패키지에서 접근이 가능하지만(단, 상속관계 일때만 적용)
		 	default	  : 같은 패키지에서만 접근 가능 <<접근 제어자가 없는 경우>>
		 	private	  : 같은 파일에서만 접근 가능
		 */
		

	}

}
